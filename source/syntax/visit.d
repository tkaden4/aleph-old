module semantics.visit;

/*
import syntax.tree;
import util;
import std.range;
import std.algorithm;

abstract class Visitor {
    ProgramNode visit(in ProgramNode node)
    {
        node.children.each!(x => this.visit(x));
    }

    StatementNode visit(in StatementNode node)
    {
    
    }
};
*/
